# Backend configuration
PORT=8000
HOST=0.0.0.0

# Comma-separated list of allowed origins for CORS. Example:
# ALLOWED_ORIGINS=http://localhost:3000,https://your-frontend.com
ALLOWED_ORIGINS=*

# Optional: AWS S3 bucket to store uploads. If omitted, files are stored locally.
S3_BUCKET=

# Optional: AWS region when S3 is used.
AWS_REGION=

# Set your model provider API key for summarisation/translation
OPENAI_API_KEY=

# If you want to disable external LLM calls during development, set to 1
MOCK_SUMMARIZER=0
